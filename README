$ pip install -r requirements.txt

$ pytest

Create a database and set SQL_ENGINE_URI variable:

$ SQL_ENGINE_URI='postgresql://postgres:@localhost/payment_db' uvicorn api:app

open http://127.0.0.1:8000/docs